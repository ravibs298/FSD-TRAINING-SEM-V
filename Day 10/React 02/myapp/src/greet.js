const Greet = ()=>{
  return (<>Hello react learners</>)
}
export default Greet;

//component composition = when we use component inside another component

//react component = a js function which returns jsx